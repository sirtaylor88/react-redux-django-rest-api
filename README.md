# Project to experiment on React, Redux and Rest API Django

To compile `static/frontend/main.js` (scripts defined in `package.json`)

  - development:
```sh
$ npm run dev
```

  - production:
```sh
$ npm run build
```
